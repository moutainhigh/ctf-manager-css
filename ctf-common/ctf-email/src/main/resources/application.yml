server:
  port: 8520
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: true
        isolation:
          thread:
            timeoutInMilliseconds: 30000
feign:
  httpclient:
    connection-timeout: 5000
ribbon:
  ReadTimeout: 30000
  ConnectTimeout: 30000